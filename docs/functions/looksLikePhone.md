[**@cartoonclouds/phone-normaliser v0.1.0**](../README.md)

***

# Function: looksLikePhone()

> **looksLikePhone**(`raw`): `boolean`

Defined in: [utils/phone/validatePhone.ts:76](https://gitlab.com/good-life/glp-frontend/-/blob/main/packages/phone-normaliser/src/utils/phone/validatePhone.ts#L76)

Cheap guard to avoid parsing obviously invalid strings.

## Parameters

### raw

`string`

The raw input string to check

## Returns

`boolean`

- True if the string looks like a phone number
